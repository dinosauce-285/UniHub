---
description: Implement a specific UniHub task using the Blueprint docs as source of truth
---

Apply a concrete requested task.

1. Read the relevant `blueprint/specs/*.md` file and `blueprint/design.md`.
2. Read current code in `server/` and `client/` before editing.
3. Implement only the requested Blueprint-backed behavior.
4. Keep changes small, production-ready, and aligned with existing patterns.
5. Do not modify `blueprint/` unless explicitly requested.
6. Verify with focused commands when feasible.
7. Report changed files, verification, assumptions, and remaining gaps.
