---
description: Produce a small implementation plan from the UniHub Blueprint docs
---

Create a bounded plan from the Blueprint source of truth.

1. Read the relevant files under `blueprint/`.
2. Treat `TASK.MD` as context, not authority.
3. Map the requested scope to `server/` and `client/` modules.
4. Recommend a safe implementation order with tests or manual checks.
5. Do not create planning folders or modify application code.
6. Do not edit `blueprint/` unless the user explicitly asks.
