---
description: Explore UniHub Blueprint docs and repo state without editing files
---

Investigate only. Do not modify files.

1. Read `blueprint/proposal.md`, `blueprint/design.md`, and relevant `blueprint/specs/*.md`.
2. Use `TASK.MD` only as planning context.
3. Inspect `server/` and `client/` only as needed.
4. Report architecture, current implementation state, inconsistencies, assumptions, and next files to read.
5. Do not create folders, implementation code, or documentation changes.
